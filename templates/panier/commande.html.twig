{% extends 'base.html.twig' %}

{% block title %}Sym_The_district!
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
	{% for p in panier %}
	<p>{{p.plat.libelle}} - {{p.quantity}}</p>
	
	{% endfor %}
	<p>TOTAL: {{total}}</p>
	{# {{ form_start(form) }}
    <h1 class="">Passer la commande</h1>
		<h3 class="fs-base fw-normal text-boby text-uppercase pb-1 pb-sm-1">
			<span class="text-decoration-underline ms-1">
				Détail d'expédition
			</span>
		</h3>
		<p>
			Si vous n'avez d'adresse ajouter en une dans votre espace compte
			<a href="#">Ajouter une adresse</a>
		</p>
		<h3 class="">Choisir votre livraison</h3>

    <div class="row">
        <div class="col">
            {{ form_row() }}
          <h3 class="">Mode de  livraison</h3>
        </div>
        <div class="col" id="some-custom-id">
            {{ form_row(form.dueDate) }}
        </div>
    </div>
{{ form_end(form) }} #}
{% endblock %}
