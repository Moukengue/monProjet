{% extends 'base.html.twig' %}

{% block title %}SymCategorie!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>


 <!---Les categories-->
    <div class="row row-cols-1 row-cols-md-3 g-4">
    
      <a href="categorie_detail.php?categorie_id=<?php echo $categorie -> id; ?>" class="text-decoration-none">
      <div class="col">
        <div class="card">
           <h1>Nos categories  {{ categories | length }}</h1>
                 {% for categorie in categories %}
                 <span class="d-block">
                     {# {{ categorie["libelle"]|upper }}  #}
                     
                     <img src="{{ asset('image/category_actuel/') ~ categorie.image }}" />
                 </span>
                 {% endfor %}
          <div class="card-body" style="width: 500px">
            <h5 class="card-title"></h5>
          </div>
        </div>
      </div>
    </a>
     
    </div>

    <!--fin categorie-->
{% endblock %}
